#include "main.h"

/**
 * _isupper - a Function that checks for uppercase character
 *
 * @c: the character to check
 *
 * Return: return 1 if 'c' uppercase otherwise Always 0 (Success)
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
return (0);
}
