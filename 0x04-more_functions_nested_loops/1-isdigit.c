#include "main.h"

/**
 * _isdigit - a Function that checks for a digit
 *
 * @c: the character to check
 *
 * Return: return 1 or 0
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	return (1);
return (0);
}
